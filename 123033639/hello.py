name = input("What's your name? ").strip().title()


#split user's name into first name and last name and use {first} to print only the first name
first, last = name.split(" ")


#remove whitespace from str if there were added extra spaces
#name = name.strip()
#formats output
#name = name.capitalize()

"""
combination
name = name.strip().title()
"""

print(f"Hello, {first}") # use f before the first string in order to print a variable in {}





