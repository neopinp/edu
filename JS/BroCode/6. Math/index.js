// Math = built - in object that provides a collection of properties and methods 

console.log(Math.PI);
console.log(Math.E);

let x = 3.21;
let y = 2;
let z;

z = Math.round(x); // round 3.21 to 3 
console.log(z)

let m = 3.99
let n = 2;
let o;

o = Math.floor(m);  // round 3.99 down to 3 
console.log(o);  

// Math.ceil(x) always rounds up 
// Math.trunc(x) always gets rid of decimals portion
// Math.pow(x, y) raising x to the power of y
// Math.sqrt(x) finds square root of x 
// Math.log(x) finds natural log of x 
// Math.abs(x) absolute value 
// Math.max(x, y, z) finds the maximum of x, y, z 
